services:
  taxibot:
    build: .
    container_name: taxibot-luxembourg
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
